#include <bits/stdc++.h>
using namespace std;
using ll = long long;
ll n, m, q;
char mp[1003][1003];
int main() {
    scanf("%lld%lld", &n, &m);
    getchar();
    for (int i = 1; i <= n; ++i)
        for (int j = 1; j <= m + 1; ++j) scanf("%c", &mp[i][j]);
    scanf("%lld", &q);
    while (q--) {
    }
    return 0;
}